type Query {
  items: [Item]
  item(id: String!): Item
}

type Mutation {
  createItem(
    title: String!
    place: String!
    description: String!
    image: String
    largeImage: String
  ): Item
  updateItem(
    id: String!
    title: String
    place: String
    description: String
  ): Item!
  deleteItem(id: String!): Item!
  signUp(name: String!, email: String!, password: String!): User
}

type Item {
  title: String
  place: String
  description: String
  image: String
  largeImage: String
  _id: String
}

type User {
  name: String!
  email: String!
  resetToken: String
  resetTokenExpiry: Float
}