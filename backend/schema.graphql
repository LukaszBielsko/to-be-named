type Query {
  getStreetArts: [Item]
}

type Mutation {
  createItem(title: String, place: String, description: String, image: String, largeImage: String): String
  delete(title: String): String
}

type Item {
  title: String
  place: String
  description: String
  image: String
  largeImage: String
}
