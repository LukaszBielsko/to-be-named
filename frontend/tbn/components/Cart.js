import React from 'react';
import CartStyles from './styles/cartStyles';

const Cart = props => (
  <CartStyles open={false}>
    <p>this is your cart, man</p>
  </CartStyles>
);

export default Cart;
